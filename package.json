{
    "name": "privschool-lms",
    "private": true,
    "version": "0.1.0",
    "type": "module",
    "workspaces": [
        "packages/*",
        "services/*",
        "common"
    ],
    "scripts": {
        "dev": "turbo run dev",
        "build": "turbo run build",
        "lint": "turbo run lint",
        "test": "turbo run test"
    },
    "devDependencies": {
        "@vitejs/plugin-react": "^4.3.4",
        "turbo": "^1.10.0",
        "vite": "^6.2.3"
    },
    "dependencies": {
        "react-router-dom": "^7.5.0"
    }
}
